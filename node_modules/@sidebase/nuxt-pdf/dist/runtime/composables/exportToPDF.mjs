import htmlToPDF from "./htmlToPDF.mjs";
export default async function(fileName, element, documentOptions, htmlOptions) {
  const pdf = await htmlToPDF(element, documentOptions, htmlOptions);
  return pdf.save(fileName);
}
