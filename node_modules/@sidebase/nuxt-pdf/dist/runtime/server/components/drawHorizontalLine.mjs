export function drawHorizontalLine(doc, moveDown = 1) {
  doc.moveDown(moveDown).moveTo(doc.options.margins.left, doc.y).lineTo(doc.page.width - doc.options.margins.right, doc.y).stroke().moveDown(moveDown + 0.5);
}
